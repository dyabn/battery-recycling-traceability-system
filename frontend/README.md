# Frontend

Vue 3 + TypeScript + Vite initialization for the battery recycling traceability system.

## Local Run

```powershell
cd frontend
npm install
npm run dev
```

The dev server runs on `http://localhost:5173` and proxies API requests to `http://localhost:8080`.
