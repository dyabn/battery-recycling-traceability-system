# 第一切片低保真原型

这是验证需求的低保真原型，不是正式前端代码。

打开 `index.html` 可查看 C4 第一切片原型。原型使用静态 HTML、CSS 和少量 JavaScript 演示：

- 正常入库闭环；
- 重复原始编码；
- 批次提交失败；
- 待补充资料；
- 验收不通过；
- 无效仓库或库位；
- 越权操作；
- 已生效记录禁止删除。

原型不连接真实数据库，不调用正式 API，不代表最终页面视觉设计。

